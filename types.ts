
export interface CalendarDay {
  day: number;
  position?: { top?: string; left?: string };
  name: string;
  emoji: string;
  whatItDoes: string;
  whenToUse: string;
  prompt: string;
  proTips: string[];
}
